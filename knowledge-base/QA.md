# 第一集验收记录

验收日期：2026-07-19

- 内容与模板测试：11 项全部通过。
- 功能截图：第 2 至第 8 页均使用 WorkBuddy 界面截图，并标出要查看或点击的位置。
- 文字遮挡：字幕独占底部 108px 安全区，8 个场景中点逐帧检查无正文、按钮或界面截图被字幕覆盖。
- 页眉：8 页全部显示 `WORKBUDDY · EP 01`；页码单独显示 `1 / 8` 至 `8 / 8`。
- 旁白：8 段全部使用 `Fun-CosyVoice3-0.5B` 与固定女声 `female_question_reference_999`，参考哈希为 `7909B83D2DEC0D5C9699994DCAF4873C0C9C3EBD9F20B98B55D76C6EEE154033`。
- 音色相似度：最低 `0.6683`，最高 `0.7295`，全部高于 `0.60` 门槛；语速保持 `1.0`，按完整语义段生成并保留自然句尾转折。
- 屏幕文字：未出现“本视频为 AI 女声合成”。
- PPT：8 页，溢出检查通过。
- HyperFrames：运行时、布局、动效和对比度检查通过；仅保留单文件偏长的维护性警告，不影响渲染或播放。
- 成片：`1920×1080`、`30fps`、H.264、AAC、48kHz 双声道，时长 `128.938667s`，大小 `45,864,997` 字节。
- 音量：平均 `-13.7 dB`，峰值 `-0.8 dB`，留有峰值余量。
- 完整性：FFmpeg 全片解码通过，无损坏帧或音轨错误。
